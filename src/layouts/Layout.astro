---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  image?: string;
  noindex?: boolean;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
}

const {
  title = 'Who’s Older? | Daily Age Guessing Game',
  description = 'Guess who is older between Indonesian public figures. New daily set, mobile-friendly, privacy-first.',
  canonical = Astro.url.href,
  image = `${Astro.url.origin}/og-image.svg`,
  noindex = false,
  structuredData = []
} = Astro.props as Props;

const jsonLd = Array.isArray(structuredData)
  ? structuredData.filter(Boolean)
  : structuredData
    ? [structuredData]
    : [];
---

<!doctype html>
<html lang="id">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		<link rel="canonical" href={canonical} />
		<meta name="theme-color" content="#0f172a" />
		{noindex && <meta name="robots" content="noindex, nofollow" />}

		<meta property="og:type" content="website" />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonical} />
		<meta property="og:image" content={image} />
		<meta property="og:locale" content="id_ID" />

		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={image} />

		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;800&display=swap" rel="stylesheet">

		{jsonLd.map((schema) => (
			<script type="application/ld+json">
				{JSON.stringify(schema)}
			</script>
		))}

		{import.meta.env.PUBLIC_CF_BEACON_TOKEN && (
			<script
				defer
				src="https://static.cloudflareinsights.com/beacon.min.js"
				data-cf-beacon={`{"token":"${import.meta.env.PUBLIC_CF_BEACON_TOKEN}"}`}
			></script>
		)}
	</head>
	<body class="bg-slate-950 text-white flex flex-col min-h-screen font-['Outfit',sans-serif] selection:bg-fuchsia-500 selection:text-white overflow-x-hidden">
		<div class="fixed inset-0 -z-10 h-full w-full bg-slate-950 bg-[radial-gradient(circle_500px_at_50%_200px,#3b0764,transparent)]"></div>
		<div class="flex-grow">
			<slot />
		</div>
		<footer class="py-8 text-center text-sm text-slate-400 border-t border-slate-800/50 mt-auto backdrop-blur-sm">
			<div class="space-x-4 font-medium">
				<a href="/about" class="hover:text-white transition-colors">About</a>
				<a href="/privacy" class="hover:text-white transition-colors">Privacy</a>
				<a href="/contact" class="hover:text-white transition-colors">Contact</a>
				<a href="/attribution" class="hover:text-white transition-colors">Attribution</a>
				<a href="/arsip" class="hover:text-white transition-colors">Archive</a>
			</div>
			<div class="mt-4 opacity-60">
				&copy; {new Date().getFullYear()} Siapa Lebih Tua?
			</div>
		</footer>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
